#include <bits/stdc++.h>

using namespace std;

int main() {
    freopen("SUBMAX.INP", "r", stdin);
    freopen("SUBMAX.OUT", "w", stdout);

    int n;
    cin >> n;

    int arr[n];
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
    }


}
